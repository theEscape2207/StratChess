# Local measurements

Local instrument: `Run-EloMatch.ps1`, clang-cl on the dev machine. Every run appends one row here.

Each row names its own reference, so two rows are comparable only when that reference matches —
this ledger mixes anchor rows, merge-base rows and compiler comparisons. **No row here is ever
comparable with a Linux CI row**: different compiler, different machine, different book.

| Date | Candidate | Reference | Games | TC | Elo +/- err | Verdict |
|---|---|---|---|---|---|---|
| 2026-07-03 | candidate-6e96e89+dirty | elo-reference-v1 | 20 | 10+0.1 | n/a | discarded |
| 2026-07-03 | candidate-b4cd8b5+dirty | elo-reference-v1 | 20 | 10+0.1 | -17.39 +/- 104.93 | smoke |
| 2026-07-03 | candidate-1c05192 | elo-reference-v1 | 500 | 10+0.1 | 25.06 +/- 25.54 | calibration |
| 2026-07-03 | candidate-1c05192+dirty | elo-reference-v1 | 500 | 10+0.1 | -27.85 +/- 27.11 | calibration |
| 2026-07-03 | candidate-1c05192 (pooled) | elo-reference-v1 | 1000 | 10+0.1 | -1.4 (pooled batches 1+2) | calibration |
| 2026-07-03 | candidate-fd8b665+dirty | elo-reference-v1 | 20 | 10+0.1 | 70.44 +/- 131.59 | smoke |
| 2026-07-23 | candidate-1a2a97c+dirty (Threads=4) | candidate-threads1 (Threads=1) | 500 | 10+0.1 | 128.55 +/- 28.36 | gain |
| 2026-07-26 | candidate-1cfc32c+dirty (mop-up eval, #70) | elo-reference-v1 | 480 of 500 planned (491 actually finished, see note) | 10+0.1 | 15.94 +/- 27.62 | inconclusive @ 480 |
| 2026-07-27 | candidate-bce4d95+dirty (rook open-file, #126) | elo-reference-v1 | 500 | 10+0.1 | 23.66 +/- 25.70 | inconclusive @ 500 |
| 2026-07-29 | candidate-9394bad (tapered eval, #99 + #118 item 4) | elo-reference-v1 | 2000 | 10+0.1 | 13.90 +/- 13.44 | inconclusive @ 2000 |
| 2026-07-29 | candidate-08d4ef8 (bishop pair + connected rooks + castling, #111/#114/#115) | elo-reference-v1 | 288 | 10+0.1 | 54.74 +/- 35.67 | gain |
| 2026-07-29 | candidate-08d4ef8 (bishop pair + connected rooks + castling, #111/#114/#115) | **`main` @ 56b3f45** | 378 | 10+0.1 | 38.76 +/- 29.86 | gain |
| 2026-07-31 | clang-cl ThinLTO @ 64a36a9 | MSVC LTCG @ 64a36a9 | 20 | 10+0.1 | -0.00 +/- 172.63 | smoke |
| 2026-08-01 | clang-cl ThinLTO @ 64a36a9 (#84) | MSVC LTCG @ 64a36a9 | 622 | 10+0.1 | 27.43 +/- 24.00 | gain |
| 2026-08-01 | clang-cl ThinLTO @ 64a36a9 (#84) | MSVC LTCG @ 64a36a9 | 3500 | 10+0.1 | **40.28 +/- 9.81** | gain |
| 2026-08-03 | elo-reference-v2 (same binary) | elo-reference-v2 | 20 | 10+0.1 | -107.54 +/- 127.62 | calibration |
| 2026-08-15 | candidate-ed05170+dirty (officer captures, #306) | **merge-base `main` @ 97340a5** | 214 | 10+0.1 | 269.09 +/- 53.27 | gain |
| 2026-08-16 | candidate-dacaab0+dirty (#237 qsearch prerequisites, pre-review-fix) | **merge-base `main` @ 97340a5** | 1370 | 10+0.1 | 39.22 +/- 16.44 | non-regression |
| 2026-08-16 | candidate-223870f+dirty (#237 qsearch prerequisites) | **merge-base `main` @ 97340a5** | 1434 | 10+0.1 | 36.48 +/- 15.82 | non-regression |
| 2026-08-17 | candidate-792f11b (abort unwind guard, #299/#310) | **merge-base `main` @ 369ce5f** | 500 | 10+0.1 | -13.21 +/- 23.72 | inconclusive @ 500 |
| 2026-08-18 | candidate-7a580e2 (same-key TT store policy, #319) | **merge-base `main` @ 090e3f8** | 500 | 10+0.1 | 36.26 +/- 23.20 | inconclusive @ 500 |
| 2026-08-18 | candidate-54c3eb4 (same-key TT store policy, #319) | **merge-base `main` @ 090e3f8** | 530 | 10+0.1 | 46.82 +/- 23.14 | gain |
| 2026-08-19 | candidate-8343867 (fifty-move rule, #345) | **merge-base `main` @ 506ae62d9284** | 2000 | 10+0.1 | 14.43 +/- 11.77 | inconclusive @ 2000 |
| 2026-08-22 | candidate-1d8956b+dirty | 54d3e54dbac873cc6209646ff6a989991ffab1d2 | 20 | 10+0.1 | -52.51 +/- 126.90 | smoke |
| 2026-08-25 | candidate-3840612+dirty (quiescence may use MAIN TT entries, #337) | **`main` @ 3840612** | 500 | 10+0.1 | 35.56 +/- 23.25 | inconclusive @ 500 |
| 2026-08-27 | candidate-86f2159 (LMR reduces by legal moves searched, #401) | **`main` @ ceebfd5** | 500 | 10+0.1 | -4.86 +/- 23.26 | inconclusive @ 500 |
| 2026-08-27 | candidate-e395433 (quiescence evasions ordered by history, #320) | **#401 branch tip @ 22572d5** | 1082 | 10+0.1 | 12.53 +/- 15.06 | non-regression |
| 2026-08-27 | candidate-e395433 (quiescence evasions ordered by history, #320) | **#401 branch tip @ 22572d5** | 19980 | 10+0.1 | **24.47 +/- 3.66** | gain |
| 2026-08-27 | candidate-a5cbd5e (two-tier SEE capture ordering, PR 2 of #86) | **`main` @ 6a265bc** | 500 | 10+0.1 | 32.05 +/- 22.83 | inconclusive @ 500 |
| 2026-08-31 | candidate-e60f177 | acafa19 | 100 | 10+0.1 | -10.43 +/- 47.92 | discarded |
| 2026-08-31 | candidate-b96698e (#128 part 2: scaled rook endings, wrong-bishop fortress, mop-up defender gate) | acafa19 (merge-base) | 700 | 10+0.1 | 10.43 +/- 18.33 | inconclusive @ 700 |
| 2026-08-31 | candidate-b7289a8 (#128 part 3: endgame scale factors) | acafa19 (merge-base) | 700 | 10+0.1 | 3.47 +/- 19.69 | inconclusive @ 700 |
| 2026-09-02 | candidate-2aafd6f (#97 PR 2) | f6e9cfb | 700 | 10+0.1 | -10.92 +/- 21.46 | inconclusive @ 700 |
| 2026-09-02 | candidate-b38d390 (#97 PR 2, half table scale) | f6e9cfb | 700 | 10+0.1 | 9.43 +/- 19.49 | inconclusive @ 700 |
| 2026-09-03 | candidate-0b5f3fb (#97 PR 3: king attack pressure) | 9cdd52e (merge-base) | 2000 | 10+0.1 | 12.69 +/- 11.04 | inconclusive @ 2000 |

## Row detail

Same order as the table above. A row with nothing to add beyond its verdict has no section here.

### 2026-07-03 — candidate-6e96e89+dirty (20 games)

smoke — FAILURES, discard

### 2026-07-03 — candidate-1c05192 (500 games)

sanity batch 1 (identical builds)

### 2026-07-03 — candidate-1c05192+dirty (500 games)

sanity batch 2 (identical builds; +dirty = batch-1 log row only)

### 2026-07-03 — candidate-1c05192 (pooled) (1000 games)

**sanity baseline PASS** — 378W/382L/240D, 49.80%

### 2026-07-23 — candidate-1a2a97c+dirty (Threads=4) (500 games)

**Gate 3 PASS** — Lazy SMP threads=4 vs threads=1, same binary; LOS 100.00%, 286W/109L/105D (67.70%); row appended manually — the `Run-EloMatch.ps1` wrapper process was killed by the harness immediately after fastchess finished (before its own Add-Content step), verified against the raw match log

### 2026-07-26 — candidate-1cfc32c+dirty (mop-up eval, #70) (480 of 500 planned (491 actually finished, see note) games)

**PARTIAL — match killed mid-run, not a script/engine failure.** `Run-EloMatch.ps1`/fastchess were terminated by what all available evidence points to as a background-task duration cap in the execution tooling (~60 minutes elapsed between match start and last log write, to the second); no error, exception, or crash text in either the fastchess log or the redirected transcript, and 491/500 "Finished" lines are present with no illegal-move/disconnect/adjudication anomalies — this is a harness/environment stoppage, not a `FAILURES, discard` case. No final fastchess summary was ever printed (match ended between periodic reports), so the last **periodic** summary — printed at the 480-game mark — is recorded here rather than a true final tally: LOS 87.17%, DrawRatio 36.25%, 199W/177L/104D (52.29%), Ptnml(0-2) [32, 39, 87, 39, 43]. Per project-owner decision, accepted as the final measurement for this validation rather than re-running (re-running risks the same cap). Mop-up's effect is expected to be concentrated in rare pawnless decisive endgames largely absent from a standard-opening-book batch, so a result within error of 0 (as here: +15.94 ± 27.62, well inside its own error bar) is consistent with "no measurable change in typical play," the expected outcome, not a sign the feature failed

### 2026-07-27 — candidate-bce4d95+dirty (rook open-file, #126) (500 games)

**SPRT NonRegression [-5, 0] — INCONCLUSIVE, hit the 500-game cap without crossing a bound** (LLR 0.76 of ±2.94, 25.8%). 204W/170L/126D (53.40%), LOS 96.51%, DrawRatio 35.60%, Ptnml(0-2) [26, 44, 89, 52, 39]. **Read this as "no regression detected", not as "+23.66 Elo"** — the 95% interval is [-2.04, +49.36] and still contains zero, so the point estimate is suggestive (LOS 96.5%) but not a demonstrated gain; the change is a 5 cp static-eval shift and was never expected to be resolvable at this sample size. Deciding it either way needs wider bounds or a much larger budget, which is not worth it for a correctness fix. `+dirty` is accurate but harmless here: the only uncommitted engine-source delta vs `bce4d95` was comment text in `Eval.cpp` (verified — no non-comment lines), so the binary is behaviourally identical to the commit. Wall time 00:40:41 at `-Concurrency 6`

### 2026-07-29 — candidate-9394bad (tapered eval, #99 + #118 item 4) (2000 games)

**SPRT Gain [0, 10] — INCONCLUSIVE, hit the 2000-game cap without crossing a bound** (LLR 1.90 of ±2.94, 64.4% of the way toward accepting H1). 842W/762L/396D (52.00%), LOS 97.89%, DrawRatio 37.80%, PairsRatio 1.16, Ptnml(0-2) [139, 149, 378, 161, 173], nElo 15.78 +/- 15.23. Wall time 02:41:56 at `-Concurrency 6`. **Read this as "positive, not yet decided" — not as "+13.90 Elo".** The 95% interval is [+0.46, +27.34]: it excludes zero only barely, and the test was still 36% short of a decision when the budget ran out. What it does establish is a *direction*: the eval-reviewer flagged middlegame king centralization as this change's main regression risk (the mg king table is file-blind while the eg table peaks centrally, so the blend crosses zero around phase 14), and a real regression there would have pushed this negative. It did not. Deciding [0, 10] properly needs roughly 4000-6000 games; the earlier run at the same settings was killed and restarted after the mop-up gate was re-keyed onto the loser's phase, so this figure covers the final code only

### 2026-07-29 — candidate-08d4ef8 (bishop pair + connected rooks + castling, #111/#114/#115) (288 games)

**SPRT Custom [-5, 15] — H1 accepted at 288 games, LLR 3.01, wall time 00:23:07.** 138W/93L/57D (57.81%), LOS 99.89%, DrawRatio 38.89%, PairsRatio 1.75, Ptnml(0-2) [13, 19, 56, 22, 34], nElo 62.84 +/- 40.13. **This row does NOT measure the three eval terms.** Like every row here it compares against the fixed `elo-reference-v1` anchor, so what H1 establishes is that **`main` + these terms, together, beat the anchor by more than 15** — the 95% interval [+19, +90] lies entirely above `elo1`. A test on that sum cannot decompose it into "main's existing margin" plus "this PR's delta", so no part of the +54.74 is attributable here. It decided in 23 minutes precisely *because* the true difference sits far above the midpoint of 5, which is the signature of a comparison dominated by something much larger than the quantity of interest. Nor can the delta be recovered by subtracting anchor rows: the tapered-eval row above is itself **inconclusive** (+13.90 with interval [+0.5, +27.3]), and differencing two noisy anchor measurements compounds their errors to roughly ±38 — less constrained than either input. Resolving a ~10-20 Elo change requires measuring it directly against `main`, which is the `main-56b3f45` row below. `+dirty` on the candidate label was this file only (the auto-appended row) — engine sources verified clean, so the binary is exactly `08d4ef8`

### 2026-07-29 — candidate-08d4ef8 (bishop pair + connected rooks + castling, #111/#114/#115) (378 games)

**SPRT Custom [-5, 15] — H1 ACCEPTED at 378 games, LLR 2.97, wall time 00:30:52.** 169W/127L/82D (55.56%), LOS 99.49%, DrawRatio 39.68%, PairsRatio 1.38, Ptnml(0-2) [15, 33, 75, 27, 39], nElo 45.96 +/- 35.02. **This is the row that measures the change** — reference is the merge-base `main` build, not the fixed anchor, so the delta is attributable to this PR. Binaries verified to differ only in `Eval.cpp`/`Eval.h` (the `UCIHandler.cpp` delta is two extra rows in the `eval` command, never executed during play; `EvalTests.cpp` is not linked into the engine). **Read this as "the three terms are worth having", not as "+38.76 Elo".** The 95% interval is [+8.90, +68.62]: it excludes zero comfortably, but the point estimate carries the usual small-sample inflation and its lower bound is what matches the eval-reviewer's +10-20 prior. Attribution among the three terms was deliberately traded away for budget (one SPRT instead of three) — per the bundling guidance in `EloMeasurement.md`, do not credit any single term. The gain is evaluation quality, not search efficiency. An initial 3-position probe suggested the candidate reached depth 11 on 21.75% fewer nodes, but that was a **sampling artifact**: widening to 5 term-isolating positions gives a per-position spread of **-44% to +229%** and an aggregate of **-1.6%**, i.e. no systematic change. Node count at fixed depth is chaotic under small eval perturbations — a 20-30 cp shift can flip the PV and thus the whole tree — so it cannot be sampled meaningfully at this scale, and notably the deltas do not track each position's net root contribution. Move ordering never consults static eval in any case (`Sort.cpp` has no reference to `Evaluate`); eval feeds only the leaf return, the qsearch stand-pat cutoff, its alpha raise, and delta pruning. So the terms pay a real **2.03% nps** cost, buy no node reduction, and still won the SPRT — the strength comes from scoring positions better. `+dirty` on the label was this file only (the row appended by the preceding anchor run) — engine sources verified clean

### 2026-07-31 — clang-cl ThinLTO @ 64a36a9 (20 games)

**Smoke only — not a measurement.** 9W/9L/2D. Run purely to confirm fastchess could drive binaries living outside the normal `x64/Release` layout (both sit in a throwaway `bench/` tree). At 20 games the error bar is +/-172, so the Elo figure carries no information whatsoever and must not be quoted.

### 2026-08-01 — clang-cl ThinLTO @ 64a36a9 (#84) (622 games)

**SPRT Custom [-5, 15] — H1 ACCEPTED at 622 games, LLR 2.98 (101.3%), wall time 00:50:19.** 268W/219L/135D (53.94%), LOS 98.79%, DrawRatio 36.01%, PairsRatio 1.37, Ptnml(0-2) [38, 46, 112, 59, 56], nElo 31.38 +/- 27.30. **This row is the DECISION, not the estimate** — read it as "worth more than 15 Elo", not as "+27.43 Elo"; the 95% interval is [+3.43, +51.43]. The estimate is the 3500-game row below, which supersedes this number. **Both sides are the same source at `64a36a9`** — only the compiler and its LTO mode differ (MSVC `/GL /LTCG` vs clang-cl `-flto=thin` with `lld-link`), so the delta is attributable to code generation alone. Equivalence was verified before the match: at Threads=1 both binaries visit identical nodes and return identical best moves on all 8 `Run-Bench.ps1` positions. `[-5, 15]` was chosen over `-Sprt Gain` deliberately: `Gain` is `[0, 10]`, and the tapered-eval row above hit its 2000-game cap still inconclusive on those bounds. Wider bounds decide faster, and `elo1 = 15` is the more useful question — adopting a second toolchain has real cost, so what matters is whether the gain is material. `+dirty` on the auto-appended label was this file plus untracked scratch directories; `git status -- StratEngine StratChessEvolved CMakeLists.txt` was empty, so the binary is exactly `64a36a9`.

### 2026-08-01 — clang-cl ThinLTO @ 64a36a9 (#84) (3500 games)

**Fixed batch (no SPRT) — this is the ESTIMATE row. Wall time 04:44:55 at `-Concurrency 6`.** 1525W/1121L/854D (55.77%), LOS 100.00%, DrawRatio 36.91%, PairsRatio 1.62, Ptnml(0-2) [173, 248, 646, 368, 315], nElo 47.69 +/- 11.51. 95% interval **[+30.5, +50.1]**. Run because the SPRT's +/-24 was too wide to act on; a fixed batch was the right tool because SPRT optimises for deciding a hypothesis, not for estimating a value, and its early stop leaves the point estimate subject to optional-stopping bias. **Do NOT pool this with the 622-game SPRT row** — pooling a stopped sample into a fixed batch drags that bias along. This row alone is the estimate; the SPRT row alone is the decision. **The two are consistent, not contradictory**: +40.28 falls inside the SPRT's [+3.43, +51.43], so the earlier figure was simply noisy and landed low. (A prediction that the batch would land *below* +27.43 on optional-stopping grounds was wrong — that effect is real but second-order, and sampling noise at 622 games dominated it.) **Quote this as "+40 Elo at 10+0.1", not as "+40 Elo".** The nps side measured +23.32% (2,798,491 -> 3,451,020 aggregate, 5 runs each, per-config spread 0.27-1.11%), which is 0.302 doublings; at the conventional ~60 Elo/doubling that predicts ~18 Elo. The measured result implies **~133 Elo per doubling**, far above the textbook figure. The likely cause is the time control: speed advantages are amplified at 10+0.1, where an engine is more often one iteration short. Expect a smaller gain at longer controls; this was not measured. **What this does not settle is the cost of adoption** — VS's ClangCL toolset has no LTO integration at all (`-flto` and `lld-link` were wired by hand), `WholeProgramOptimization=true` under it is a trap that produces a smaller binary while passing no `-flto`, and the clang legs only compiled with `-Wno-error` because this engine has only ever been `/W4 /WX`-clean against MSVC. See #84.

### 2026-08-03 — elo-reference-v2 (same binary) (20 games)

**Re-pin verification — carries no strength information.** Both sides were the *same file*, SHA256 `D0B4F7F4…B308D6A`, so the true difference is exactly zero by construction. Run to confirm the new default reference resolves from cache and fastchess drives it; both did. The ±127.62 interval spans zero more than eight times over, which is the point: a 20-game smoke resolves nothing, and reading its point estimate as a result is the mistake this row exists to illustrate. The `+dirty` in the auto-generated label was uncommitted doc/script edits in the worktree — the binary is exactly `df9245f`

### 2026-08-15 — candidate-ed05170+dirty (officer captures, #306) (214 games)

**SPRT Gain [0, 10] — H1 ACCEPTED at 214 games, LLR 2.95 (100.1%), wall time 00:16:14.** 159W/20L/35D (82.48%), LOS 100.00%, DrawRatio 14.95%, PairsRatio 17.20, Ptnml(0-2) [1, 4, 16, 27, 59], nElo 347.16 +/- 46.55. **Two corrections were applied to this row by hand; the auto-generated version was wrong on both counts.** (1) The Reference column said `elo-reference-v2`. It was not: the run passed `-ReferenceExe` pointing at a build of the merge base, and `logs/elo/<stamp>/config.json` records `cmd: bench\before.exe`. `Run-EloMatch.ps1` labels the reference side from `-ReferenceTag` regardless of `-ReferenceExe` (**#309**), which inverts the distinction this file depends on — an anchor row measures cumulative standing, a merge-base row measures one change. This is a merge-base row, so the delta **is** attributable to #306. (2) The auto row was stamped `— FAILURES, discard`. That was a false positive and the measurement is sound: the detector's pattern is `illegal`, `disconnect`, `stall`, `loses on time`, and `illegal` matched 167 `Warning; Illegal PV move` lines (**#310**, a reporting defect fastchess tolerates — it warns and plays on from `bestmove`). The log contains **zero** disconnects, stalls, time losses or crashes, and fastchess exited 0. **Read this as "#306 is worth far more than 10 Elo", not as "+269.09 Elo".** The 95% interval is [+215.8, +322.4] and the SPRT stopped as soon as it could, so the point estimate carries the usual optional-stopping inflation; no fixed batch was run to turn it into an estimate, because nothing about the decision needed one. The size is unsurprising given the defect: `ComputeCaptures()` never generated a knight, bishop, rook, queen or king capture, so quiescence resolved only pawn captures and promotions and every hanging officer at the horizon was scored statically. Re-bench after merging #313, depth 12, `Threads=1`, built-in set (contract 1; current `main` @ `6dcb9a1` vs branch @ `1b447bd`): main-tree nodes 24,269,611 -> 19,582,469 (**-19%**, deterministic), qsearch nodes 3,337,940 -> 13,854,611 (**+315%**), total nodes 27,607,551 -> 33,437,080 (**+21%**), and qsearch share 12.1% -> 41.4%. Across the interleaved sweep, `main` was stable at 9.31s while the candidate ranged 11.79-12.79s; because the candidate timing drifted, quote wall time as **+26-37%** and total-node nps as **-4-12%**, not a single point. The old pre-#312 nps figure counted only the shrinking main tree while charging qsearch time; contract 1 makes the added work explicit. `+dirty` on the label was the untracked `bench/` directory holding the two comparison binaries plus this file; engine sources were verified clean, so the candidate binary is exactly `ed05170`. **This row supersedes nothing, but it re-frames everything above it**: every prior row, and the pinned `elo-reference-v2` itself, measured an engine whose quiescence was blind to officer captures

### 2026-08-16 — candidate-dacaab0+dirty (#237 qsearch prerequisites, pre-review-fix) (1370 games)

**SPRT NonRegression [-5, 0] — H1 ACCEPTED at 1370 games, LLR 2.95 (100.1%), wall time 01:47:53.** 603W/449L/318D (55.62%), LOS 100.00%, DrawRatio 33.58%, PairsRatio 1.47, Ptnml(0-2) [74, 110, 230, 130, 141], nElo 44.29 +/- 18.40. **Superseded by the row below — kept because the pair is the evidence.** This measured the branch before three review findings were addressed (the fifty-move half of the quiescence draw check, which was wrongly gated on `in_check`; lazy evaluation of the LMR checking-move test; a formatting fix). Those changes altered draw handling on a path the tactical suite barely exercises, so the two rows bracket them: +39.22 before, +36.48 after, each comfortably inside the other's interval. That is the evidence the fixes cost no strength — a claim nothing else in the validation set could support. The run was launched, then cancelled when the review landed; fastchess survived the cancellation of its parent shell and played to completion, which is why the row exists at all. See the row below for the two hand-corrections that apply to both

### 2026-08-16 — candidate-223870f+dirty (#237 qsearch prerequisites) (1434 games)

**SPRT NonRegression [-5, 0] — H1 ACCEPTED at 1434 games, LLR 2.95 (100.2%), wall time 01:54:09.** 627W/477L/330D (55.23%), LOS 100.00%, DrawRatio 34.31%, PairsRatio 1.51, Ptnml(0-2) [80, 108, 246, 148, 135], nElo 41.79 +/- 17.98. **Four changes measured together, deliberately**: never reduce a checking move in LMR; seed the repetition history with the position a board is set up from and count a return to the root as a draw inside the search; legal quiescence while in check (#237 stage 2); and draw detection on the quiescence path. They are bundled because the intermediate states are configurations that will never ship — stage 2 alone scores 35/36 on the tactical suite, all four score 36/36 — so per-change SPRTs would have measured engines we do not intend to build. Per-defect attribution was established separately, by tactical-suite bisection rather than by Elo. **Read as "not a regression, and probably worth ~35 Elo"** — the interval is [+20.7, +52.3] and excludes zero, but the test stopped as soon as it could, so the point estimate carries optional-stopping inflation. Bench at depth 12, four interleaved runs each: nodes 34,477,205 -> 24,269,611 (**-30%**, deterministic). **The two timing figures first recorded here were wrong and have been replaced.** They read "derived time to depth -31% (13.26s -> 9.09s)" and "nps 2,599,715 -> 2,669,389 (+2.7%)". Both were *derived* as `nodes / nps`, which is only valid when both come from the same run; the reference-side nps was taken during the machine drift this row already documents, so the derivation inherited it and inverted the nps sign. Re-measured directly on 2026-08-16, two interleaved runs each, 0.5% spread: **wall clock 10.78s -> 8.78s (-18.6%)** and nps 3.20M -> 2.77M (**-13.5%**). Note both node and nps figures here are main-tree only (#312), which is also why the nps fell while the clock improved: this change moves work out of the quiescence tree, and the old counter could not see that. **The strength result above is unaffected** — it came from 1434 games, not from bench. An earlier non-interleaved pair suggested nps -7.4%; that was machine drift between batches (`main` benched 2.94M then and 2.60M minutes later), not a property of the change — only interleaved runs are comparable here. **Two hand-corrections, applying to this row and the one above.** (1) The Reference column read `elo-reference-v2`; both runs passed `-ReferenceExe` and `logs/elo/config.json` records `cmd: bench\before.exe`. `Run-EloMatch.ps1` labels the reference from `-ReferenceTag` regardless (**#309**), which inverts the anchor-vs-merge-base distinction this file depends on. These are merge-base rows, so the delta is attributable. (2) Both were stamped `— FAILURES, discard`: the detector's pattern is `illegal`, `disconnect`, `stall`, `loses on time`, and `illegal` matched 1058 and 1211 `Illegal PV move` warnings (**#310**, a reporting defect fastchess tolerates). Neither log contains a disconnect, stall, time loss or crash. `+dirty` on both labels was the untracked `bench/` directory holding the comparison binaries plus this file

### 2026-08-17 — candidate-792f11b (abort unwind guard, #299/#310) (500 games)

**SPRT NonRegression [-5, 0] — INCONCLUSIVE, hit the 500-game cap without crossing a bound** (LLR -0.37, -12.4%). 144W/163L/193D (48.10%), LOS 13.69%, DrawRatio 35.20%, PairsRatio 0.84, Ptnml(0-2) [26, 62, 88, 53, 21], nElo -17.00 +/- 30.45, wall time 00:41:05 at `-Concurrency 6`. **The `— FAILURES, discard` stamp on the auto-generated row was a false positive and has been removed by hand — this is the third row to need that correction.** The detector's pattern is `illegal`, `disconnect`, `stall`, `loses on time`, and `illegal` matched 385 `Warning; Illegal PV move` lines. The log contains **zero** disconnects, stalls, time losses or crashes, all 500 games finished, and fastchess exited 0. **Those 385 warnings are this row's most useful result, and every one of them names `main-369ce5f` — the pre-fix side. The candidate produced none.** That is #310 measured in the setting it was reported from: 0.77 illegal PV lines per game from the old binary, 0.00 from the new one, over 500 games. (The candidate does produce 14 `PV continues after threefold repetition` warnings; that is a different, benign diagnostic — the line repeats a position and every move in it is legal.) **On strength, read this as "no regression demonstrated, and none demonstrated absent either" — not as "-13 Elo".** The 95% interval is [-36.93, +10.51] and contains zero; the point estimate is negative but the test moved only 12% toward H0 in 500 games, which at this sample size is noise, not a signal. Deciding [-5, 0] properly needs roughly 2000-4000 more games (3-5.5 h at this rate) and was judged not worth it for a correctness fix whose fixed-depth output is *bit-identical* to the reference: 4 positions at depth 7 and the 8-position bench set at depth 12 give identical node counts, scores, PVs and best moves, so any real difference can only come from what the two binaries do on the abort path under a clock — TT, PV and killer/history content — which is exactly what the change alters and what no fixed-depth check can cover. Bench nps delta over two interleaved runs per side: +0.005%. Reference labelling is accurate here despite **#309** (`-ReferenceTag` names the reference regardless of `-ReferenceExe`): the tag was set to the merge-base commit deliberately, and both binaries were fingerprinted before the run — the reference reproduces the illegal `pv d7d5 e4d5 d5e4 …` at `go nodes 10000`, the candidate does not

### 2026-08-18 — candidate-7a580e2 (same-key TT store policy, #319) (500 games)

**SPRT Gain [0, 10] — INCONCLUSIVE, hit the 500-game cap without crossing a bound** (LLR 2.23, 75.8% of the way to H1). 177W/125L/198D (55.20%), LOS 99.90%, DrawRatio 32.80%, PairsRatio 1.51, Ptnml(0-2) [12, 55, 82, 71, 30], nElo 48.01 +/- 30.45, wall time 00:40:52 at `-Concurrency 6`. **Superseded by the row below, which decided the same question; kept because the pair shows what the opening book was costing.** This run exhausted `openings-250.pgn` exactly — 250 openings, 500 distinct games — so the game cap and the book ran out together. Read on its own: almost certainly a gain, size not pinned to the 10 Elo the test asks about. The 95% interval is [+13.1, +59.5] and excludes zero and LOS is 99.90%, but the formal verdict is inconclusive and this row is not a decision. The reference is a **merge-base** build passed as `-ReferenceExe`, so the delta is attributable to this change alone; `-ReferenceTag` was set to the merge-base commit to keep the label honest under **#309**. Supporting evidence, not part of the SPRT: `Run-Bench.ps1` at depth 12, `Threads=1`, both binaries clang-cl Release measured back to back — nodes 28.41M → 21.64M (-23.8%), wall clock 10.140s → 7.896s, nps flat within a spread wider than the difference

### 2026-08-18 — candidate-54c3eb4 (same-key TT store policy, #319) (530 games)

**SPRT Gain [0, 10] — H1 ACCEPTED at 530 games, LLR 2.95 (100.2%)**, wall time 00:42:25 at `-Concurrency 6`. 195W/124L/211D (56.70%), LOS 100.00%, DrawRatio 31.70%, PairsRatio 1.74, Ptnml(0-2) [14, 52, 84, 79, 36], nElo 60.66 +/- 29.58. **What is established is "worth at least 10 Elo", not "+46.8"**: the test stopped as soon as it could, so the point estimate carries optional-stopping inflation — the row above, which ran to a fixed cap, put the same candidate at +36.26 and is the less biased of the two point estimates. The candidate differs from that row's `7a580e2` only in tests and comments; the binaries are behaviourally identical. **The difference that matters between the two rows is the book.** This run used `EngineTesting\openings-large.pgn` (34,700 openings, from `8moves_v3.pgn`), so all 530 games are distinct openings; the run above used the committed 250-opening smoke book, which a 500-game batch exhausts exactly. The auto-discovery in `Run-EloMatch.ps1` had never fired on this machine because the file was sitting in `EngineTesting\` under its upstream name. Reference is a merge-base build passed as `-ReferenceExe`, with `-ReferenceTag` set to the merge-base commit so the label stays honest under **#309**. Supporting evidence, not part of the SPRT: `Run-Bench.ps1` at depth 12, `Threads=1`, both binaries clang-cl Release measured back to back — nodes 28.41M → 21.64M (-23.8%), wall clock 10.140s → 7.896s, nps flat within a spread wider than the difference

### 2026-08-19 — candidate-8343867 (fifty-move rule, #345) (2000 games)

**SPRT Gain [0, 10] — INCONCLUSIVE, hit the 2000-game cap without crossing a bound** (LLR 2.61, 88.6% of the way to H1). 679W/596L/725D (52.08%), LOS 99.19%, DrawRatio 37.00%, PairsRatio 1.22, Ptnml(0-2) [84, 200, 370, 241, 105], WL/DD 1.61, nElo 18.69 +/- 15.23, wall time 02:41:43 at `-Concurrency 6`. Used `EngineTesting\openings-large.pgn`, so all 2000 games are distinct openings. **Read this as "almost certainly a real gain, size not pinned to the 10 Elo the test asks about"** — the 95% interval is [+2.7, +26.2] and excludes zero, and LOS is 99.19%, but the formal verdict is inconclusive and this row is not a decision. Deciding [0, 10] properly needs roughly another 500 games; because the match ran to completion rather than being interrupted, `-ResumeDir` does not apply, so that means a fresh ~2500-game run (≈3.4 h at this rate). Judged not worth it for a change that lands on correctness grounds regardless. **The effect is expected to be smaller than the defect sounds**, because the pre-fix emergency move is chosen from the head of the generated move list and is usually a pawn move, which resets the clock and self-heals after one bad move; the exception is pawnless endgames, where nothing resets it and the reference plays near-random moves until the game ends. Reference is a merge-base build passed as `-ReferenceExe`, with `-ReferenceTag` set to the merge-base commit so the label stays honest under **#309**. Supporting evidence, not part of the SPRT: `Compare-SearchEquivalence.ps1` is **IDENTICAL** across 90 lines, 6 positions at depth 12, `Threads=1` — the builtin positions never approach either threshold in 12 plies, so every game above differs only where the halfmove clock actually got high

### 2026-08-25 — candidate-3840612+dirty (quiescence may use MAIN TT entries, #337) (500 games)

**SPRT NonRegression [-5, 0] — INCONCLUSIVE, hit the 500-game cap without crossing a bound** (LLR 1.33, 45.2% of the way to H1). 173W/122L/205D (55.10%), LOS 99.87%, DrawRatio 38.40%, PairsRatio 1.57, Ptnml(0-2) [15, 45, 96, 62, 32], nElo 46.97 +/- 30.45, wall time 00:13:51 for the resumed half at `-Concurrency 6`. **Read as strong evidence against regression, not as a measured "+35.6"**: the 95% interval [+12.3, +58.8] excludes zero and LOS is 99.87%, but the formal verdict is inconclusive and this row is not by itself a decision to ship. Used `EngineTesting\openings-large.pgn`, so all 500 games are distinct openings. Reference is a build of `main` @ 3840612 passed as `-ReferenceExe`, with `-ReferenceTag` set to that commit so the label stays honest under **#309**. **This row was corrected by hand.** The match was interrupted at game 302 and finished with `-ResumeDir`; resume mode labels the row from the resuming invocation's parameters rather than the restored `config.json`, so it originally recorded the reference as `elo-reference-v2` (the pinned anchor, which was never played) and the kind as `match` — see **#388**. Supporting evidence, not part of the SPRT: `Run-Bench.ps1` at depth 12, `Threads=1`, both binaries clang-cl Release — total nodes 21.46M → 20.45M (-4.7%), wall 7.229s → 6.693s. That aggregate hides the shape: `rook-endgm` -41.4% and `tactical-5` -23.2% (the two positions #319 regressed, which is what this change set out to recover) against `closed-mid` **+163.2%**, a regression reproduced at depths 10/11/12/13 and not attributable to search chaos

### 2026-08-27 — candidate-86f2159 (LMR reduces by legal moves searched, #401) (500 games)

**SPRT Custom [-10, 0] — INCONCLUSIVE, hit the 500-game cap** (LLR 0.01, 0.3% of the way to either bound). 142W/149L/209D (49.30%), LOS 34.07%, DrawRatio 36.40%, PairsRatio 0.96, Ptnml(0-2) [24, 57, 91, 58, 20], WL/DD 0.94, nElo -6.38 +/- 30.45, wall time 00:39:24 at `-Concurrency 6`. **Read as "no effect resolvable at this sample size", not as a measured regression**: the 95% interval [-28.1, +18.4] contains both hypotheses, so it excludes neither. **The LLR is the informative number here, and it is the reason not to simply add games.** It sat at 0.00-0.17 for the whole run rather than drifting, because the observed score falls almost exactly in the middle of the tested band — and an SPRT whose true effect lies strictly inside [elo0, elo1] is *expected* to run to the cap, since neither hypothesis ever accumulates evidence against the other. More games would very likely buy another inconclusive row, not a verdict; deciding a change this small needs a fixed-batch estimate (size) rather than an SPRT (decision). Used `EngineTesting\openings-large.pgn`, so all 500 games are distinct openings. Reference is a build of `main` @ ceebfd5 passed as `-ReferenceExe`, with `-ReferenceTag` set to that commit so the label stays honest under **#309**. The `+dirty` suffix fastchess recorded was an untracked `bench/` directory holding before/after binaries, not a modified tracked file; it has been moved out of the worktree. Supporting evidence, not part of the SPRT: `Compare-SearchEquivalence.ps1` at depth 12, `Threads=1` is **DIFFERENT on 6 of 6** positions (node deltas +17 to +1170, identical PVs and scores), which is the intended behaviour change and establishes that the defect was pervasive rather than rare; `Run-Bench.ps1` in a single session, 20,458,694 -> 20,520,423 nodes (+0.3%), nps 2.973M -> 2.980M, best moves identical on all 8 positions

### 2026-08-27 — candidate-e395433 (quiescence evasions ordered by history, #320) (1082 games)

**SPRT Custom [-10, 0] — H1 ACCEPTED at 1082 games**, so the change is not a regression at the -10 Elo bound and the decision is made. It is a *decision*, not a size: an accepted H1 says "better than elo1 = 0", and the 95% interval [-2.5, +27.6] around the point estimate still contains zero, so this row must not be quoted as "+12.5 Elo". The fixed-batch row immediately below is the estimate. Reference is the **#401 branch tip**, not `main`: #320 sits on top of #401 and measuring against `main` would have measured the bundle. Used `EngineTesting\openings-large.pgn`, so all games are distinct openings. Run locally at `-Concurrency 6`

### 2026-08-27 — candidate-e395433 (quiescence evasions ordered by history, #320) (19980 games)

**Fixed batch (no SPRT) — this is the ESTIMATE row, and the change is a clear gain.** CI strength lab run 33022317577: 18 shards x 1110 games at concurrency 3 per shard, pooled over 9990 game *pairs* (score 53.52%), openings `UHO_4060_v3.epd`, `Threads=1`, 192 MB hash. Pooling is over pairs rather than games on purpose — pooling per-shard game counts double-counts and silently narrows the interval. An SPRT gives a decision and a fixed batch gives a size, which is why both rows exist for one change: the SPRT above settled "not a regression" in 1082 games, and this settles "how much" at an interval an SPRT of any affordable length would not have produced. Same reference and reasoning as the row above. **The candidate SHA was rewritten to c586827 by a later rebase onto the updated #401 branch; the two differ only by test-only additions under `STRAT_ENABLE_TEST_ACCESS` and comments, so the measured shipping binary is unchanged**

### 2026-08-27 — candidate-a5cbd5e (two-tier SEE capture ordering, PR 2 of #86) (500 games)

**SPRT Gain [0, 10] — INCONCLUSIVE, hit the 500-game cap** (LLR 1.99, 67.7% of the way to the H1 bound). 159W/113L/228D (54.60%), LOS 99.72%, DrawRatio 36.80%, PairsRatio 1.47, Ptnml(0-2) [13, 51, 92, 65, 29], WL/DD 0.64, nElo 43.06 +/- 30.45, wall time 00:39:38 at `-Concurrency 6`. **Read this one differently from the #401 row above: that LLR sat still, this one climbed.** A steady drift toward H1 is what an SPRT does when the true effect is at or above the tested band, so more games would most likely produce a verdict rather than a second inconclusive row — the opposite of #401, where adding games was the wrong move. The supporting numbers point the same way: the 95% interval is [+9.2, +54.9] and excludes zero, and LOS is 99.7%. It is still recorded as inconclusive, because a cap-stopped SPRT is not a decision, and the interval is far too wide to quote a size from — 32.05 is not a measured Elo gain and must not be cited as one. Used `EngineTesting\openings-large.pgn`, so all 500 games are distinct openings; `Threads=1`, 192 MB hash, both sides clang-cl builds. Reference is a build of `main` @ 6a265bc in the `see-bench-baseline` worktree passed as `-ReferenceExe`, with `-ReferenceTag` set to that commit so the label stays honest under **#309**. **The branch tip moved on after the run: comments in `See.h`/`Sort.cpp`, test-only additions, documentation, and a lint-driven rewrite of `see_ge`'s attacker chain into a table-driven loop (`bugprone-assignment-in-if-condition`). That last one is a source change, so it was gated rather than asserted — `Compare-SearchEquivalence.ps1` at depth 12, `Threads=1` is IDENTICAL across 90 lines on all 6 positions, and `Run-Bench` reports the same 15,119,653 nodes at nps 2.998M -> 3.035M. The measured search is the one being merged**

### 2026-08-31 — candidate-e60f177 (100 games)

**Discard — cancelled by hand, not an engine failure.** SPRT NonRegression [-5, 0], killed at ~100 games because an eval-reviewer finding (the #118 item 5 mop-up gate) meant the candidate was about to change. The "FAILURES" the script recorded are the in-flight games disconnecting when fastchess was terminated. Superseded by the b96698e row below, which is the same comparison against the corrected candidate

### 2026-08-31 — candidate-b96698e (#128 part 2: scaled rook endings, wrong-bishop fortress, mop-up defender gate) (700 games)

**SPRT NonRegression [-5, 0] — INCONCLUSIVE, hit the 700-game cap** (LLR 0.74, 25.0% toward H1). 211W/190L/299D (51.50%), LOS 86.79%, DrawRatio 38.86%, PairsRatio 1.18, Ptnml(0-2) [22, 76, 136, 91, 25], WL/DD 1.06, nElo 14.66 +/- 25.74, wall time 00:55:23 at `-Concurrency 6`. No time losses, illegal moves or disconnects. The 95% interval is [-7.9, +28.8], containing both zero and the tested -5 bound. The LLR wandered rather than drifted (-0.26 at 198 games, +0.27 at 600, +0.74 at 700), so unlike the #86 row above there is no reason to expect more games to produce a verdict; the estimated ~6,500 games to cross a bound was judged not worth ~9 h. Sizing is deferred to the CI strength lab across the whole of #128. Reference is the merge-base build passed as `-ReferenceExe` with `-ReferenceTag acafa19` so the label stays honest under **#309**; `EngineTesting\openings-large.pgn`, `Threads=1`, 192 MB hash, both sides clang-cl

### 2026-08-31 — candidate-b7289a8 (#128 part 3: endgame scale factors) (700 games)

**SPRT NonRegression [-5, 0] — INCONCLUSIVE, hit the 700-game cap** (LLR 0.30, 10.1% toward H1). 208W/201L/291D (50.50%), LOS 63.54%, DrawRatio 37.14%, PairsRatio 1.06, Ptnml(0-2) [31, 76, 130, 81, 32], WL/DD 0.94, nElo 4.55 +/- 25.74, wall time 00:55:59 at `-Concurrency 6`. No time losses, illegal moves or disconnects. The 95% interval is [-16.2, +23.2], containing both zero and the tested -5 bound. The LLR wandered rather than drifted (0.35 at ~250 games, 0.00 at ~550, 0.30 at 700), so as in the part 2 row above there is no reason to expect a verdict from more games — ~6,900 games, ~9 h, to cross a bound. Sizing stays deferred to the CI strength lab across the whole of #128. Supersedes the b96698e row: that commit predates every search change on this branch. Reference is the merge-base build passed as `-ReferenceExe` with `-ReferenceTag acafa19` so the label stays honest under **#309**; `EngineTesting\openings-large.pgn`, `Threads=1`, 192 MB hash, both sides clang-cl

### 2026-09-02 -- candidate-2aafd6f (#97 PR 2: king shelter, pawn storm, king-file openness) (700 games)

**SPRT NonRegression [-5, 0] -- INCONCLUSIVE, hit the 700-game cap** (LLR -0.35, 11.9% toward H0). 226W/248L/226D (48.43%), LOS 15.88%, DrawRatio 34.00%, PairsRatio 0.86, Ptnml(0-2) [45, 79, 119, 67, 40], WL/DD 1.98, nElo -13.12 +/- 25.74, wall time 00:57:55 at `-Concurrency 6`. No time losses, illegal moves or disconnects. The 95% interval is [-32.4, +10.5], containing zero, the tested -5 bound and the -9 Elo the measured nps cost alone predicts -- so this row resolves nothing, and must not be quoted as a measured regression. **The LLR wandered rather than drifted** (+0.25 at 200 games, +0.02 at 302, +0.09 at 500, -0.35 at 700; the score sat within a point of 50% for the first 500 games and dipped only in the last 200), so as in the two #128 rows above there is no reason to expect a verdict from more games -- ~5,900 games, ~8 h, to cross a bound. **What makes this row worth acting on despite being inconclusive is not the SPRT but the bench pass beside it**: the three terms cost **-5.5% nps**, which at the project's ~1.7 Elo per 1% converts to about -9 Elo of speed, and the point estimate here is -10.9. An SPRT measures a term net of its own speed cost, and this one gives no sign of the positional value paying that cost back. The two candidate causes are the cost itself -- removable only by a pawn hash (**#131**), not by the two rounds of micro-optimisation already tried, which recovered nothing -- and the ~110 cp triple-count identified in the design doc's D5a, where shelter, king-file openness and `ISOLATED_PAWN_PENALTY` all fire on one missing shelter pawn without knowing about each other. Reference is the merge-base build passed as `-ReferenceExe` with `-ReferenceTag f6e9cfb` so the label stays honest under **#309**; `EngineTesting\openings-large.pgn`, `Threads=1`, 192 MB hash, both sides clang-cl. The `+dirty` suffix in the candidate label is an untracked `bench/` directory holding the before/after binaries, not a modified tracked file. **An earlier attempt at this same comparison (logs `20260902-181117`, `20260902-181319`) was discarded unreported**: a stray 500-game anchor match was started by accident and ran concurrently with the SPRT, putting two fastchess runners at `-Concurrency 6` on one box. Both were killed and neither wrote a row

### 2026-09-02 -- candidate-b38d390 (#97 PR 2 at half table scale) (700 games)

**SPRT NonRegression [-5, 0] -- INCONCLUSIVE, hit the 700-game cap** (LLR +0.60, 20.4% toward H1). 217W/198L/285D (51.36%), LOS 82.90%, DrawRatio 35.14%, PairsRatio 1.18, Ptnml(0-2) [29, 75, 123, 94, 29], WL/DD 1.12, nElo +12.48 +/- 25.74, wall time 00:56:42 at `-Concurrency 6`. No time losses, illegal moves or disconnects. The 95% interval is [-10.1, +28.9], containing zero and excluding neither bound, so **this row resolves nothing on its own and must not be quoted as a measured gain**. It is the same candidate as the `candidate-2aafd6f` row above with `KING_SHELTER`, `KING_STORM`, `KING_FILE_HALF_OPEN` and `KING_FILE_OPEN` all halved (commit `b38d390`); same reference, same bounds, same game count, so the two rows are directly comparable. **The comparison, not either row alone, is what carries information**: the point estimate moved -10.9 -> +9.4 while an interleaved 4-round bench put the two builds at the same speed (-5.89% vs -5.56% nps against the merge base, indistinguishable), so the swing is attributable to the table scale rather than to a speed change. Two point estimates with +/-20 intervals are weak evidence and the intervals overlap across [-10.1, +10.5]; what the pair does establish is that the full-magnitude tables were not obviously the better of the two, and that at half scale the positional value at least covers its own ~-9 Elo speed cost at the point-estimate level. This is the design doc's D5a triple-count hypothesis surviving a first test, not a confirmation of it. **The LLR plateaued rather than drifted** (+0.25 at ~150 games, +0.68 at ~500, then wandering 0.48-0.68 over the last 200), giving ~3,400 games / ~4.5 h to cross a bound on the optimistic reading that the plateau resumes climbing. Sizing stays deferred to the CI strength lab across the whole of **#97**, where all four PRs can be measured together at ~+/-5 Elo. Reference is the merge-base build passed as `-ReferenceExe` with `-ReferenceTag f6e9cfb` so the label stays honest under **#309**; `EngineTesting\openings-large.pgn`, `Threads=1`, 192 MB hash, both sides clang-cl. The `+dirty` suffix in the candidate label is an untracked `bench/` directory holding the before/after binaries, not a modified tracked file

### 2026-09-03 -- candidate-0b5f3fb (#97 PR 3: king attack pressure) (2000 games)

**SPRT NonRegression [-5, 0] -- INCONCLUSIVE, hit the 2000-game cap** (LLR +2.39, 81.2% toward H1). 614W/541L/845D (51.82%), LOS 98.79%, DrawRatio 37.90%, PairsRatio 1.16, Ptnml(0-2) [59, 228, 379, 249, 85], WL/DD 1.06, nElo +17.52 +/- 15.23, wall time 02:41:23 at `-Concurrency 6`. No time losses, illegal moves or disconnects. **This row is inconclusive by the SPRT's own rule and must not be quoted as a crossed bound** -- but unlike every #97 and #128 row above it, it is not uninformative. Two things distinguish it.

**The 95% interval is [+1.65, +23.73], which excludes zero as well as the tested -5 bound.** The formal verdict the gate asks for is a weaker claim than that interval already makes: H1 here is only "not worse than -5 Elo".

**The LLR drifted monotonically rather than wandering**: +0.18 at 100 games, +0.35 at 300, +0.71 at 500, +1.31 at 1000, +1.87 at 1400, +2.39 at 2000, with the score sitting within half a point of 52% from game 100 onward. That is the opposite of the `candidate-b38d390` row above, whose LLR plateaued -- and it means the usual `N * 2.94 / LLR` estimate is being read on a trend that actually exists: **~2,460 games, about 460 more than were played, roughly 37 minutes.** The cap stopped this run, not the instrument. It was left at 2,000 rather than re-run higher because a fresh run cannot resume a completed one -- `-ResumeDir` restores the original `-Games` -- so crossing the bound costs a full 3.5 h replay, and the CI strength lab answers a better question with the same wall clock (design doc D9).

The `Gain` leg [0, 10] was therefore not reached. It is the leg the point estimate of +12.69 actually suggests would pass, and it remains the open half of PR 3's gate.

Speed context, because an SPRT measures a term net of its own cost: the three king-attack aggregates cost **-2.5% nps** against this same reference (two order-balanced sessions of five interleaved depth-12 runs a side, ~0.4% spread), which at the project's ~1.7 Elo per 1% is about -4 Elo of speed. A point estimate of +12.7 net of that implies roughly +17 Elo of positional value before the cost -- a figure worth stating only as the arithmetic it is, not as a measurement.

Reference is the merge-base build passed as `-ReferenceExe` with `-ReferenceTag 9cdd52e` so the label stays honest under **#309**; `EngineTesting\openings-large.pgn`, `Threads=1`, 192 MB hash, both sides clang-cl. The `+dirty` suffix in the raw candidate label is an untracked `bench/` directory holding the before/after binaries, not a modified tracked file. fastchess emitted 80 "PV continues after threefold repetition" compliance warnings; those concern the reported PV only and are not in the discard set (no time loss, illegal move, disconnect or stall). **An earlier attempt at this comparison (logs `20260903-012938`, `20260903-013509`) was discarded unreported**: `Run-EloMatch.ps1 -?` does not print help under `pwsh -File` but starts a real 500-game anchor match, which ran concurrently with the SPRT for six minutes. Both were killed and neither wrote a row -- the same accident, from the same cause, as the one recorded in the `candidate-2aafd6f` row above
