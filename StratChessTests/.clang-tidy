# Catch2 REQUIRE controls optional access but is not modeled by clang-tidy.
# Test code also favors clarity over micro-optimizations.
InheritParentConfig: true
Checks: >
  -bugprone-unchecked-optional-access,
  -performance-*
